---
# ── RECORD 01 · VPRO ──────────────────────────────────────────────────
# A record holds chapters. Adding a record is adding a numbered directory
# beside this one — nothing else has to change, no registry to update, no
# route to write. The number prefix sets order and is cut off the URL:
# this directory is `01-vpro`, so the page is /vpro.

key: VPRO
name: VPro redesign
desc: A decade-old CCPM system, rebuilt from the inside.
spine: Four chapters, three years.

# The record's accent. A NAME, not a hex — each name resolves to one triple
# for light and another for dark in globals.css, because a muted olive that
# reads on paper disappears on a dark ground. Adding a colour is one name
# here and one block there. Accent shows at rest only as structure: the
# spine edge, the chapter numerals, one hairline, the paper's own tint.
accent: olive

# ── The masthead ──────────────────────────────────────────────────────
# The problem, in one line, above the fold. Not a summary of the project —
# the thing that was wrong. Assembled from the approved chapter copy (§3)
# and carrying no fact that isn't already in it. NEEDS SIGN-OFF before it
# counts as final; nothing here was invented, but nothing here is approved.
problem: >-
  A decade of history, four people who understood the code, and a config
  change that cost two days of someone else's time.

# ── The facts band ────────────────────────────────────────────────────
# The four-second read: what a hiring manager needs before deciding whether
# to scroll. NEVER behind a disclosure, never a ruled table — it renders as
# the panel idiom, tagged once in the corner if anything is unwritten.
facts:
  - key: Role
    value: Designer, junior → senior
  - key: Timeline
    value: Three years
  - key: Team
    value: team size — not written yet
    placeholder: true
  - key: Owned
    value: Configuration · Planning · Modify Subtasks
  - key: Outcome
    value: one line — not written yet
    placeholder: true
---
