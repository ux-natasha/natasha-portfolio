---
# ── THE TOP OF THE FRAME ──────────────────────────────────────────────
# Name, role, the line, and the at-a-glance card. Everything on this
# screen is editable from GitHub's web editor — no toolchain, no build
# step you have to understand.

name: Natasha M.
role: Designer, Vector Digital Labs.

# THE HOOK. Approved copy (CLAUDE.md §3) — set exactly, do not paraphrase.
# Wrap one word in _underscores_ for italic. One italic word, not three.
line: Three years, five modules, and one long _argument_ about the brightness of gray. On projectors.

# ── The at-a-glance panel ─────────────────────────────────────────────
# FOUR rows. Renders as a bordered panel, never a ruled table (§1).
# `placeholder: true` on a row styles it as unwritten; the panel's single
# corner tag is derived from the rows themselves (how many are unwritten),
# not set separately here, so it can't drift out of sync with them.
card:
  label: At a glance
  rows:
    - key: Role
      value: Junior → Senior, three years
    - key: System
      value: A decade-old CCPM system, rebuilt
    - key: Owned
      value: Configuration · Planning · Modify Subtasks
    - key: Outcome
      value: Not written yet — a real outcome, not a guess, belongs here.
      placeholder: true

# ── The pictorial ─────────────────────────────────────────────────────
# An original abstraction drawn in code. Never a product screenshot (§8).
# Three gray planes, lightest on top — the argument, settled.
plate:
  alt: Three overlapping gray planes, each lighter than the one behind it.
  caption: the brightness of gray, settled

# The line that hands off into the work.
descend: The work, in the order it was built
---
